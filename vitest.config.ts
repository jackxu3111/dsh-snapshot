/**
 * The package uses Node.js' built-in `node:test` runner. This empty module is
 * retained only for tooling that expects a conventional test-config path; no
 * Vitest dependency or runtime integration is used.
 */
export default {}
